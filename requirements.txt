# Note: Install PyTorch with CUDA support using:
# uv sync (in the src/ directory with pyproject.toml)
# or manually: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
trackio
pyyaml
dpkt